# 反向旅游 Agent｜产品与开发执行规划

> 文档用途：本文件不是概念介绍，而是交付给 Code Agent 的执行规格。Code Agent 应据此完成一个可运行、可演示、具有高完成度的一页式 C 端 AI 产品原型。
>
> 暂定产品名：**去处 / Somewhere**
> 核心文案：**把一种感觉，翻译成一个地方。**

---

## 0. 给 Code Agent 的总指令

请在当前工作区实现一个可运行的响应式 Web 页面。不要只输出静态设计稿，也不要做成普通的聊天机器人界面。页面必须能够完成一条完整的交互链路：

1. 用户通过自然语言或示例灵感描述想要的旅行氛围；
2. 产品展示 AI 对“感觉”的结构化理解；
3. 产品模拟 Agent 探索世界、比较候选目的地和核验现实约束；
4. 展示 3 个有明确差异的目的地结果；
5. 用户选择一个目的地，查看可执行的三日旅行提案；
6. 用户可以用“再孤独一点”“更暖一些”等氛围指令重新生成结果；
7. 整个流程具有完善的加载、空状态、错误状态、交互反馈和移动端适配。

默认实现为高保真前端原型，优先保证：

- 产品概念在 10 秒内能够被理解；
- 首次完整体验在 90 秒内完成；
- 页面不是“输入框 + 一堆文字”的 AI 套壳；
- 即使没有真实 API Key，也能使用内置演示数据完成全流程；
- 后续可以平滑接入真实模型、地图、天气和地点搜索服务。

---

## 1. 项目简介

### 1.1 产品定义

“反向旅游 Agent”是一款从**感受、画面和氛围**出发寻找旅行目的地的 C 端 AI 产品。

传统旅行工具要求用户先知道“去哪里”，再帮助用户查询攻略、交通和酒店。本产品反转这个过程：用户不需要知道目的地，只需要描述想进入的状态，例如：

> 我想一个人待三天。天气有点冷，可以一直走路，游客不要太多，晚上能找到安静的小酒馆。

AI 将这段模糊表达理解为一组旅行氛围与现实约束，例如：

- 情绪：安静、轻微孤独、放松；
- 环境：偏冷、潮湿、旧建筑、靠海；
- 节奏：慢、适合步行、低密度；
- 社交：可以独处，但不是完全荒凉；
- 现实约束：3 天、预算适中、交通可达。

Agent 随后探索并比较真实目的地，解释每个地点为什么匹配，最后生成一份兼顾情绪体验与现实可执行性的旅行提案。

### 1.2 一句话价值主张

**你描述一种感觉，AI 帮你在世界上找到对应的地方。**

### 1.3 核心差异

本项目不是以下产品：

- 不是输入城市后生成攻略；
- 不是通用聊天机器人；
- 不是按热门榜单推荐景点；
- 不是只根据标签匹配图库；
- 不是只有情绪价值、缺乏真实可执行信息的旅行文案生成器。

它必须同时表现两层能力：

1. **理解感受**：从自然语言中提取审美、情绪、节奏和隐含偏好；
2. **接触现实**：使用地点、天气、交通、开放时间和预算等信息验证推荐是否可行。

### 1.4 为什么适合作为 AI 产品求职项目

该项目能够集中展示：

- 从模糊需求中识别真实用户目标；
- 把生成式 AI 设计成结构化产品体验；
- Agent 的任务拆解、工具调用、进度反馈与结果合成；
- 多模态输入的扩展可能；
- 模型不确定性、来源说明与结果可解释性；
- 推荐结果的评价体系；
- C 端产品的视觉表达、分享动机和增长入口。

---

## 2. 产品目标与边界

### 2.1 MVP 目标

MVP 只验证一个核心假设：

> 用户是否愿意用“感觉”而不是“地点”作为旅行探索的起点，并认为 AI 找到的结果既有惊喜感，又足够可信和可执行。

### 2.2 MVP 成功标准

原型层面的成功标准：

- 用户无需解释即可完成首次输入；
- 用户能理解 AI 为什么推荐某个地点；
- 三个结果之间有明显差异，不是同质化列表；
- 用户可以通过氛围调节产生第二轮探索；
- 路线结果能体现现实约束，而非纯文学描述；
- 面试演示时，完整流程稳定、无需临场配置服务。

产品上线后的建议指标：

- 探索发起率；
- 从结果卡进入目的地详情的转化率；
- 二次调节氛围的比例；
- 收藏或分享率；
- “很像我想要的感觉”主观评分；
- 推荐结果中现实信息的错误反馈率；
- 用户最终实际出行或进一步搜索的意向率。

### 2.3 MVP 不做

- 不完成机票、酒店支付闭环；
- 不做复杂账户系统；
- 不做多人协同旅行；
- 不构建完整旅行社区；
- 不追求覆盖全球所有目的地；
- 不在第一版实现真实视频或音乐语义分析；
- 不声称推荐信息绝对准确。

---

## 3. 目标用户与典型场景

### 3.1 核心用户

20–35 岁、有自主旅行意愿、重视体验与氛围、厌倦标准化攻略的年轻用户。他们通常：

- 想旅行，但说不出明确目的地；
- 会被电影、照片、音乐或一句话激发出行冲动；
- 更关注“在那里会有什么感受”，而不只是景点数量；
- 希望发现非典型目的地；
- 对 AI 推荐感兴趣，但需要知道推荐理由。

### 3.2 Jobs To Be Done

> 当我想暂时离开当前生活、却不知道去哪里时，我希望描述自己想进入的状态，让产品帮我找到一个真正符合这种感觉、同时又能实际抵达的地方。

### 3.3 首版重点场景

优先支持以下四类输入：

1. **情绪逃离**：最近很累，想去安静、没有任务感的地方；
2. **画面复刻**：想去一个像某部电影或某张照片的地方；
3. **氛围探索**：想找潮湿、旧、可以一直走路的海边小城；
4. **有限约束**：从上海出发，三天，预算 4000 元，不要太热。

---

## 4. 演示案例

为了保证项目无需真实后端也能稳定演示，内置至少 3 组完整的示例数据。

### 案例 A：轻微孤独的海边小城

输入：

> 我想一个人待三天。天气有点冷，可以一直走路，游客不要太多，晚上能找到安静的小酒馆。

氛围解析：

- 安静 88%；
- 独处 82%；
- 清冷 76%；
- 步行友好 91%；
- 非热门 74%；
- 夜晚仍有生活 65%。

候选地：

- 日本函馆：海港、坡道、旧建筑、夜晚安静；
- 英国惠特比：冷峻海岸、哥特气质、适合长距离步行；
- 葡萄牙波尔图：旧城、潮湿、酒馆文化，但热闹程度稍高。

### 案例 B：海街日记式夏天

输入：

> 想找一个像《海街日记》一样的地方，有电车、海风、旧房子和很慢的夏天。

候选地：

- 日本镰仓；
- 日本松山；
- 中国福建东山岛。

### 案例 C：国内短途降噪

输入：

> 从上海出发，周末两天，不想打卡，不要古镇商业街，只想散步、喝茶、看看水。

候选地：

- 浙江松阳；
- 江苏溧阳南山周边；
- 浙江宁海。

注意：演示数据中的事实性描述应保持克制。天气、价格、开放时间等信息必须明确标注为“示例数据”或提供更新时间。

---

## 5. 核心体验流程

### 5.1 主流程

```text
进入首页
  ↓
看到一句价值主张和一个非常明确的输入区
  ↓
输入旅行感觉 / 点击示例灵感
  ↓
AI 解析氛围，用户可修改解析结果
  ↓
Agent 展示探索世界的过程
  ↓
返回 3 个差异化候选目的地
  ↓
用户调节氛围或选择一个地点
  ↓
展开目的地沉浸详情与三日提案
  ↓
收藏 / 分享 / 重新探索
```

### 5.2 关键产品原则

- **先让用户感到被理解，再展示结果。**
- **显示必要的 Agent 进度，但不暴露冗长思维链。**
- **推荐理由必须对应用户刚刚表达的偏好。**
- **结果中同时存在“惊喜”和“证据”。**
- **允许用户修改 AI 的理解，而不必重新输入整段文字。**
- **现实约束冲突时，明确指出取舍。**

### 5.3 Agent 进度文案

加载过程不要使用虚假的技术日志或“AI 正在思考”。采用用户可理解的任务状态：

1. 正在理解你想要的旅行感觉；
2. 正在寻找气候与节奏相似的地方；
3. 正在排除交通不便或过度热门的结果；
4. 正在核对季节、预算和步行体验；
5. 找到 3 个值得去看看的地方。

每一步持续约 500–900ms，完整演示加载不超过 4 秒。支持“减少动态效果”系统设置。

---

## 6. 功能规划

### 6.1 P0：必须完成

#### A. 灵感输入

- 多行自然语言输入；
- 4 个示例灵感短句；
- 支持回车快捷提交，保留换行输入方式；
- 字数提示，建议 10–300 字；
- 空输入和过短输入提示；
- 输入框不是传统聊天气泡，应像一张“旅行委托卡”。

#### B. 氛围理解

提交后显示结构化理解结果：

- 情绪：安静、浪漫、孤独、松弛、冒险等；
- 环境：海边、山地、城市、乡村、旧建筑、现代等；
- 节奏：慢 / 中 / 快；
- 人群：独处 / 轻社交 / 热闹；
- 气候：偏冷 / 温和 / 偏热；
- 现实约束：出发地、时长、预算、签证或国内限定。

至少提供两种可编辑方式：

- 点击标签删除；
- 使用氛围调节按钮或滑杆增减倾向。

#### C. Agent 探索进度

- 以任务步骤或地图扫描的视觉方式展示；
- 当前步骤清晰；
- 已完成步骤有反馈；
- 不展示模型私有推理过程；
- 用户可以取消并返回输入。

#### D. 目的地推荐

同时返回 3 个候选地，每张卡包含：

- 城市 / 地区名称；
- 国家或省份；
- 一句话氛围描述；
- 与用户需求的匹配度；
- 3 个匹配理由；
- 1 个明确的“不完全匹配点”；
- 建议旅行时长；
- 预算等级；
- 推荐季节或当前气候提示；
- 一张有氛围的封面图或本地渐进式占位图。

三张卡必须有角色差异：

- **最像你描述的地方**；
- **更意外的选择**；
- **更容易抵达的选择**。

#### E. 氛围微调

提供快捷控制：

- 再安静一点；
- 更温暖一些；
- 少一点商业化；
- 更容易抵达；
- 预算降低；
- 只看国内。

点击后不应整页刷新。保留原始意图，展示参数变化，再重新进入短版探索流程。

#### F. 目的地详情

选择地点后，在同一页面展开沉浸式详情，不跳转到完全不同的网站结构。内容包括：

- “为什么是这里”；
- 氛围匹配雷达或分布条；
- 适合用户的一天；
- 三日轻路线；
- 现实提醒；
- 可替代地点；
- 保存与分享按钮。

#### G. 完整状态

必须实现：

- 初始状态；
- 输入聚焦状态；
- 解析中；
- 解析完成；
- 搜索中；
- 结果状态；
- 二次搜索；
- 详情展开；
- 空结果；
- 模拟网络错误；
- 移动端导航与结果布局。

### 6.2 P1：建议实现

- 图片拖拽上传的 UI 与本地预览；
- 图片输入在无模型时映射到预置演示案例；
- 出发地、预算、天数的轻量补充表单；
- 结果收藏到 localStorage；
- 分享结果时生成可复制摘要；
- 深色 / 浅色主题中至少选择一个并做到完整，不要求双主题；
- 页面重载后恢复最近一次演示状态；
- “为什么推荐”证据抽屉。

### 6.3 P2：后续扩展

- 真实图片语义分析；
- 音乐链接或歌单输入；
- 地图路线；
- 实时天气；
- 机票与酒店价格；
- 多人偏好合并；
- 旅行后的反馈学习；
- 个人旅行审美档案；
- 用户公开分享的“氛围地图”。

---

## 7. 页面信息架构

这是一个**单页产品体验**，不是传统 Landing Page 拼接。页面建议分为以下区域。

### 7.1 顶部导航

- 左侧：品牌标识“去处 / Somewhere”；
- 中间或右侧：我的收藏、原理说明；
- 主按钮：换一种感觉；
- 页面滚动后保持轻量悬浮，但不要使用厚重 SaaS 导航栏。

### 7.2 Hero / 输入舞台

首屏只做一件事：让用户描述想去的“感觉”。

建议文案：

```text
把一种感觉，翻译成一个地方。
不必先知道去哪里。告诉我，你想进入怎样的天气、节奏和生活。
```

输入区包含：

- 大尺寸委托文本框；
- 图片上传入口；
- 示例灵感；
- “替我寻找”主按钮；
- 一行隐私与信息时效提示。

### 7.3 氛围翻译层

用户提交后，Hero 不消失，而是向下展开成“AI 对你的理解”：

- 左侧为原始描述；
- 右侧为标签、氛围强度和现实约束；
- 用户可以直接修正；
- 底部按钮：“理解得差不多，开始找”。

### 7.4 世界探索层

采用地图、经纬网、扫描线或路线轨迹构成视觉核心。此处承担 Agent 进度反馈：

- 地图只是表达探索范围，不需要第一版集成复杂地图 SDK；
- 可以使用纯 CSS / SVG 绘制抽象地图或轨迹；
- 展示任务状态，不展示思维链；
- 加载结束后地图上的 3 个坐标成为结果卡的视觉锚点。

### 7.5 目的地结果层

桌面端采用非对称三栏：主推荐卡占据更大面积，另外两张上下错落。移动端采用纵向卡片。

卡片需要支持：

- 鼠标悬停时显示更多匹配依据；
- 点击后从页面下方或侧面展开详情；
- 当前选中地点与地图坐标联动；
- 图片加载失败时仍保持高级视觉效果。

### 7.6 详情层

详情不是旅行攻略列表，而是一篇可操作的旅行提案：

1. 开场：一句未来场景；
2. 为什么这里与你匹配；
3. 在这里度过的一天；
4. 三日低压力路线；
5. 现实条件与取舍；
6. 相似但不同的备选地；
7. 收藏、分享、重新探索。

### 7.7 页尾

页尾只保留简短产品说明：

> 推荐由 AI 根据你的描述生成。天气、价格、营业时间与交通可能变化，请在预订前再次核验。

---

## 8. 文字线框图

```text
┌─────────────────────────────────────────────────────────────┐
│ 去处 SOMEWHERE                         收藏  原理  换一种感觉 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  把一种感觉，翻译成一个地方。                                │
│  不必先知道去哪里。                                          │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 我想一个人待三天。天气有点冷，可以一直走路……          │  │
│  │                                                       │  │
│  │  ＋加入一张图片                        [替我寻找 →]    │  │
│  └───────────────────────────────────────────────────────┘  │
│  灵感：海街日记式夏天 / 国内两日降噪 / 冷冷的海边城市      │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ AI 对你的理解                                               │
│ 原始委托              安静 88%  清冷 76%  步行 91%          │
│                       独处 82%  夜晚有生活 65%               │
│                       3天 / 预算适中 / 游客较少              │
│                                      [开始探索世界 →]        │
├─────────────────────────────────────────────────────────────┤
│                    抽象世界地图 / Agent 进度                 │
│              ○ 函馆        ○ 惠特比       ○ 波尔图          │
│        正在排除过度热门或不适合步行的目的地……               │
├─────────────────────────────────────────────────────────────┤
│ [主推荐：函馆             ] [意外选择：惠特比              ]│
│ [大图 + 氛围 + 匹配依据   ] [更易抵达：波尔图              ]│
│                                                             │
│ 再找一次： [再安静一点] [更暖] [少商业化] [只看国内]         │
├─────────────────────────────────────────────────────────────┤
│ 展开：如果你在函馆待三天……                                  │
│ 为什么是这里 / 一天体验 / 三日路线 / 现实提醒 / 分享         │
└─────────────────────────────────────────────────────────────┘
```

---

## 9. 视觉与设计风格

### 9.1 核心方向

视觉主题：**当代旅行杂志 × 地理档案 × 电影感公路笔记**。

页面应该像一份正在被 AI 生成的私人旅行特刊，而不是科技公司控制台。整体气质：克制、安静、有触感、略带未知感。

记忆点：用户提交一句感觉后，这句话仿佛成为一枚坐标，页面沿着坐标在世界地图上寻找对应地点。

### 9.2 色彩

建议使用偏纸张质感的浅色方案：

- 背景：暖灰白 / 旧纸色 `#F2EFE8`；
- 主文字：近黑色 `#1E211D`；
- 次级文字：苔藓灰 `#666C62`；
- 主强调色：旅行邮戳红 `#D6533C`；
- 辅助色：深海蓝绿 `#1E5B58`；
- 分隔线：半透明石墨灰；
- 避免紫色科技渐变、霓虹蓝大光晕和玻璃拟态卡片堆叠。

所有颜色使用 CSS Variables 统一管理，并通过对比度检查。

### 9.3 字体

- 中文正文优先使用有阅读感的衬线或人文黑体组合；
- 英文展示标题可使用有编辑感的 serif；
- 避免 Arial、Inter、Roboto 和默认系统字体直接作为视觉主角；
- 如使用在线字体，必须提供可靠回退字体，避免离线时布局崩溃；
- 数字、坐标、预算等信息可以使用等宽字体作为小范围点缀。

可考虑：

- 中文标题：思源宋体 / Noto Serif SC；
- 中文正文：霞鹜文楷或 Noto Sans SC 的克制使用；
- 英文标题：Cormorant Garamond / DM Serif Display；
- 数据标记：IBM Plex Mono。

Code Agent 可根据网络与项目依赖选择最终字体，但应保持“旅行编辑部”气质。

### 9.4 图像

- 图像承担氛围，不承担事实证明；
- 使用统一的低饱和、轻颗粒、电影截图式处理；
- 图片需要统一裁切比例；
- 使用轻微色温遮罩让不同来源图片保持一致；
- 不要在一张卡里堆多个旅游图库小图；
- 无图片时使用地图纹理、色块和地名排版保证页面仍成立。

如未提供可合法使用的图片资源，优先使用远程可替换占位配置或自行绘制的抽象视觉，不要把带版权水印的图片提交进仓库。

### 9.5 动效

重点设计一个完整的“探索发生”时刻：

- 首屏文字和输入框分层出现；
- 提交后，氛围标签像坐标注释一样依次生成；
- 世界探索阶段出现路线轨迹和坐标脉冲；
- 三张目的地卡按空间顺序进入；
- 选中地点时，地图坐标与卡片同步响应；
- 详情展开使用平滑布局变化，不做突兀路由跳转。

动效原则：

- 主要过渡 250–450ms；
- Agent 演示流程总时长不超过 4 秒；
- 支持 `prefers-reduced-motion`；
- 不使用持续旋转的装饰性 3D 球体；
- 不让动效阻止用户继续操作。

### 9.6 响应式

- 桌面端：非对称编辑排版，充分利用横向空间；
- 平板：结果卡改为主卡 + 横向列表；
- 手机：单列信息流，氛围标签可横向滑动，底部操作按钮可吸附；
- 最窄支持 360px；
- 所有可点击区域至少约 44px；
- 不允许出现横向页面溢出。

---

## 10. AI 与 Agent 实现方案

### 10.1 Agent 工作流

建议逻辑上拆分为四个阶段，但 MVP 不需要真的部署四个独立 Agent：

```text
用户委托
  ↓
Vibe Interpreter：把模糊描述翻译成结构化旅行偏好
  ↓
Place Explorer：检索并召回候选目的地
  ↓
Reality Checker：核对天气、交通、预算、季节与开放信息
  ↓
Journey Editor：比较候选地并生成可阅读的最终旅行提案
```

原则：先用一次或少量结构化模型调用完成 MVP，避免为了“多 Agent”而增加脆弱性。UI 可以展示工作阶段，但不能伪造模型逐字思维过程。

### 10.2 结构化输入

```ts
type TravelBrief = {
  rawInput: string;
  imageUrl?: string;
  origin?: string;
  durationDays?: number;
  budgetCny?: {
    min?: number;
    max?: number;
  };
  domesticOnly?: boolean;
  travelMonth?: string;
};
```

### 10.3 氛围解析输出

```ts
type VibeProfile = {
  summary: string;
  emotions: Array<{
    label: string;
    score: number;
  }>;
  environments: string[];
  pace: "slow" | "balanced" | "active";
  socialDensity: "solitary" | "light" | "lively";
  climate: {
    temperature: "cold" | "mild" | "warm" | "any";
    texture?: Array<"dry" | "humid" | "foggy" | "windy" | "sunny">;
  };
  mobility: {
    walkabilityImportance: number;
    transitPreference?: string;
  };
  constraints: {
    origin?: string;
    durationDays?: number;
    budgetCnyMax?: number;
    domesticOnly?: boolean;
  };
  ambiguities: string[];
};
```

### 10.4 目的地结果结构

```ts
type DestinationRecommendation = {
  id: string;
  city: string;
  region: string;
  country: string;
  coordinates?: { lat: number; lng: number };
  role: "best-match" | "unexpected" | "easy-to-reach";
  matchScore: number;
  tagline: string;
  atmosphere: string[];
  matchReasons: Array<{
    preference: string;
    evidence: string;
  }>;
  tradeoff: string;
  suggestedDays: number;
  budgetLevel: "low" | "medium" | "high";
  seasonNote: string;
  realityStatus: {
    weatherCheckedAt?: string;
    transitCheckedAt?: string;
    needsUserVerification: string[];
  };
  heroImage?: string;
  itinerary: ItineraryDay[];
};

type ItineraryDay = {
  day: number;
  theme: string;
  intro: string;
  moments: Array<{
    timeOfDay: "morning" | "afternoon" | "evening";
    title: string;
    description: string;
    practicalNote?: string;
  }>;
};
```

### 10.5 工具接口预留

真实版本可预留以下 adapter，不在 UI 组件中直接调用第三方 API：

```ts
interface PlaceSearchProvider {
  searchCandidates(profile: VibeProfile): Promise<PlaceCandidate[]>;
}

interface WeatherProvider {
  getForecast(placeId: string, dateRange?: DateRange): Promise<WeatherSummary>;
}

interface TransitProvider {
  estimateAccess(origin: string, destination: string): Promise<TransitSummary>;
}

interface ImageProvider {
  getDestinationImage(placeId: string): Promise<ImageAsset | null>;
}
```

MVP 提供 `mock` 实现。真实实现通过环境变量启用，API Key 不得写入前端源码或提交到 Git。

### 10.6 模型提示词原则

系统提示应包含：

- 不直接重复用户原话，要抽象出偏好；
- 不以热门程度作为主要推荐依据；
- 每个推荐必须指出一个取舍；
- 不确定信息必须明确标注；
- 不生成无法核验的精确价格、天气或营业时间；
- 推荐结果必须有差异；
- 输出严格遵循 JSON Schema；
- 对高风险、极端天气、签证和安全问题提示用户二次核验。

示例任务提示：

```text
你是一名旅行氛围研究员。你的工作不是直接生成攻略，而是把用户模糊的旅行愿望翻译成可检索的偏好，再推荐三个具有明显差异的地点。

三个地点分别承担：最佳匹配、意外发现、更容易抵达。每个地点必须解释与用户偏好的对应关系，并指出至少一个现实取舍。不要编造实时事实。无法确认的内容写入 needsUserVerification。
```

---

## 11. 推荐逻辑与评价机制

### 11.1 推荐评分

示例评分，不要求第一版实现复杂算法：

```text
总匹配度 =
  30% 氛围匹配
+ 20% 环境与气候匹配
+ 15% 节奏与人群密度匹配
+ 15% 可达性
+ 10% 预算适配
+ 10% 季节适配
```

结果排序不能只依赖总分。必须通过结果角色保证差异化和探索感。

### 11.2 AI 评价集

至少建立 12 条测试输入，覆盖：

- 只有情绪，没有现实约束；
- 只有预算与时间，没有情绪；
- 同时包含冲突需求；
- 只看国内；
- 极低预算；
- 模糊的影视作品描述；
- 用户明确不想去热门地；
- 用户要求不可行；
- 输入过短；
- 中英文混合；
- 潜在危险地区要求；
- 与旅行无关的输入。

每条结果人工评价：

- 是否正确理解氛围；
- 是否遵守硬约束；
- 三个推荐是否有差异；
- 推荐理由是否能回指用户输入；
- 是否存在编造事实；
- 是否正确表达不确定性；
- 行程是否符合用户想要的节奏。

### 11.3 最低验收线

- 硬约束遵守率 ≥ 95%；
- 推荐理由可追溯率 ≥ 90%；
- 三结果差异化通过率 ≥ 85%；
- 明显事实编造为 0；
- UI 演示流程错误率为 0。

这些指标用于项目展示和测试设计，不代表已经通过真实线上实验。

---

## 12. 安全、可信与隐私

- 不将推荐描述为专业安全判断；
- 极端天气、战争、灾害、签证和健康风险必须提示查询官方信息；
- 实时信息展示更新时间；
- 预算为估算区间，不给出虚假的精确总价；
- 图片上传默认只用于本次分析，UI 中解释数据处理方式；
- 不要求用户提供身份证、护照号等敏感信息；
- 用户分享页面前，移除出发地等可能暴露隐私的信息；
- 所有外部链接打开前清楚显示来源；
- 模型或工具失败时提供可理解的降级结果。

---

## 13. 技术实现建议

### 13.1 推荐技术栈

若当前目录没有既有项目，采用：

- Vite；
- React；
- TypeScript；
- CSS Modules、原生 CSS 或 Tailwind 三选一，优先服从现有仓库；
- Framer Motion 仅在依赖允许且确有需要时使用；
- Lucide React 用于基础图标；
- Vitest + Testing Library；
- Playwright 用于关键演示路径；
- ESLint + Prettier。

不要为了展示 AI 能力引入沉重的后端框架。若接真实模型，再增加一个轻量 server route / serverless function，用于保护 API Key 和校验结构化输出。

### 13.2 建议目录

```text
src/
  app/
    App.tsx
    app-state.ts
  components/
    navigation/
      TopNav.tsx
    brief/
      TravelBriefComposer.tsx
      InspirationChips.tsx
      ImageDropzone.tsx
    vibe/
      VibeTranslation.tsx
      VibeTag.tsx
      VibeControls.tsx
    exploration/
      ExplorationStage.tsx
      AbstractWorldMap.tsx
      AgentProgress.tsx
    destinations/
      DestinationGrid.tsx
      DestinationCard.tsx
      DestinationDetail.tsx
      MatchBreakdown.tsx
      Itinerary.tsx
    shared/
      Button.tsx
      StatusMessage.tsx
      ErrorBoundary.tsx
  data/
    demo-scenarios.ts
    destinations.ts
  hooks/
    useTravelExploration.ts
    useReducedMotion.ts
    useLocalStorage.ts
  services/
    travel-agent.ts
    mock-travel-agent.ts
    providers.ts
  types/
    travel.ts
  styles/
    tokens.css
    globals.css
    motion.css
  tests/
    app-flow.test.tsx
    travel-agent.test.ts
public/
  images/
```

可根据仓库实际结构调整，但必须维持清晰的 UI、状态、数据和服务边界。

### 13.3 页面状态机

建议使用显式状态，避免多个布尔值互相冲突：

```ts
type ExplorationStatus =
  | "idle"
  | "interpreting"
  | "reviewing-vibe"
  | "exploring"
  | "showing-results"
  | "refining"
  | "showing-detail"
  | "empty"
  | "error";
```

状态切换应可测试，且刷新后的恢复行为明确。

### 13.4 Demo 与真实服务切换

```ts
const travelAgent = import.meta.env.VITE_USE_REAL_AGENT === "true"
  ? createRemoteTravelAgent()
  : createMockTravelAgent();
```

要求：

- 默认启动即为 Demo 模式；
- Demo 模式没有任何网络依赖；
- 演示延时可配置；
- 错误状态可通过开发开关触发；
- README 说明如何接入真实服务，但不要要求评审者配置 Key 才能看见结果。

### 13.5 性能

- 首屏避免加载全部大图；
- 图片使用懒加载和明确尺寸；
- 非关键动画不阻塞交互；
- 避免引入大体积地图 SDK；
- 构建无 TypeScript 错误；
- 浏览器控制台无明显错误和重复警告。

---

## 14. Code Agent 的多 Agent 分工建议

Code Agent 可以创建 **3 个子 Agent** 并行工作，主 Agent 负责项目初始化、整合、最终决策和验收。最多同时运行 3 个子 Agent，避免超过常见并发槽位。各 Agent 必须遵守文件所有权，避免同时修改同一文件。

### 14.1 子 Agent A：`experience_ui`

职责：

- 根据本规划实现设计系统与核心视觉组件；
- 重点负责 Hero、氛围翻译、探索地图、目的地卡片和响应式布局；
- 实现动效与 reduced-motion；
- 确保页面有明确的“旅行编辑部”视觉语言。

建议文件所有权：

```text
src/components/**
src/styles/**
```

禁止事项：

- 不改服务层与 Demo 数据结构；
- 不自行引入新的全局状态方案；
- 不把页面改成聊天 UI；
- 不使用紫色渐变科技风作为默认方案。

交付：核心 UI 组件、样式、响应式实现和组件使用说明。

### 14.2 子 Agent B：`agent_flow`

职责：

- 实现类型定义、状态机、Demo 数据和 mock Agent；
- 实现氛围解析、探索进度、结果返回和二次调节的逻辑；
- 提供稳定的三个演示场景；
- 保证未来可以替换为真实服务。

建议文件所有权：

```text
src/types/**
src/data/**
src/services/**
src/hooks/useTravelExploration.ts
src/app/app-state.ts
```

禁止事项：

- 不修改视觉样式；
- 不在组件内硬编码大段目的地数据；
- 不调用需要暴露 API Key 的前端接口。

交付：可独立测试的 mock Agent、类型、数据与状态逻辑。

### 14.3 子 Agent C：`quality_accessibility`

第一阶段先只读检查规格和项目结构；A、B 完成并由主 Agent 整合后，再启动实际测试与修复。职责：

- 编写核心流程测试；
- 检查键盘操作、焦点、语义标签、颜色对比度；
- 检查 360px、768px、1440px 三档布局；
- 检查加载、空结果、错误和 reduced-motion；
- 运行构建、测试与 lint；
- 报告并修复明确的问题。

建议文件所有权：

```text
src/tests/**
e2e/**
```

如需修复他人文件，应先将问题和具体文件告知主 Agent，避免并发覆盖。

交付：测试、问题清单、修复记录和最终验证结果。

### 14.4 主 Agent 负责

- 检查当前仓库与既有规范；
- 初始化项目和依赖；
- 确定最终技术选型；
- 创建共享类型和组件接口契约，或批准 Agent B 的契约；
- 维护 `App.tsx` 和整体页面装配；
- 协调文件边界，处理冲突；
- 对照本文件逐项验收；
- 运行最终测试与视觉检查；
- 更新 README；
- 不接受只有静态首屏、没有完整交互的交付。

### 14.5 推荐执行顺序

```text
主 Agent：检查仓库 → 初始化 → 建立基础契约
                    ↓
         ┌──────────┴──────────┐
         ↓                     ↓
Agent A：视觉与组件     Agent B：状态、数据、mock Agent
         └──────────┬──────────┘
                    ↓
主 Agent：整合完整流程与页面
                    ↓
Agent C：测试、可访问性、响应式检查
                    ↓
主 Agent：修复、视觉 QA、最终验收
```

若 Code Agent 无法创建子 Agent，则按同样顺序单线程执行，不得跳过测试与视觉验收。

---

## 15. 开发里程碑

### Milestone 1：可运行骨架

- 项目可以安装、启动和构建；
- 完成设计 tokens、全局样式和基础页面结构；
- 完成显式状态模型；
- 接入第一组 Demo 数据。

验收：输入示例后可从 idle 走到结果状态，即使页面样式尚未完成。

### Milestone 2：核心体验

- 完成输入、氛围解析、Agent 进度、3 个目的地结果；
- 完成氛围微调和二次探索；
- 完成目的地详情与三日路线；
- Demo 流程全程无需网络。

验收：完整演示可重复执行 5 次无错误。

### Milestone 3：视觉与动效

- 建立明确的旅行编辑视觉；
- 完成非对称结果布局；
- 完成探索地图、坐标与卡片联动；
- 完成主要动效和 reduced-motion；
- 完成移动端布局。

验收：360px、768px、1440px 无明显错位或横向溢出。

### Milestone 4：质量与交付

- 补齐空状态、错误状态、图片失败状态；
- 完成单元测试和关键路径 E2E；
- 完成键盘和可访问性检查；
- 完成 README；
- 生成可用于作品集的截图或短录屏。

验收：构建、测试、lint 全部通过，控制台无明显错误。

---

## 16. 测试清单

### 16.1 功能

- [ ] 点击示例会正确填入输入框；
- [ ] 空输入不能提交；
- [ ] 正常输入进入氛围解析；
- [ ] 用户可以删除或调整标签；
- [ ] Agent 进度按顺序显示；
- [ ] 搜索完成后显示 3 个不同结果；
- [ ] 每个结果都有匹配点和取舍；
- [ ] 氛围微调会更新结果状态；
- [ ] 点击目的地可展开详情；
- [ ] 收藏在刷新后仍存在；
- [ ] 错误状态可以重试；
- [ ] Demo 模式断网仍可使用。

### 16.2 视觉

- [ ] 首屏 10 秒内能理解产品；
- [ ] 页面没有常见 AI 紫色渐变；
- [ ] 输入区不像聊天软件；
- [ ] 结果视觉层级明确；
- [ ] 三个结果有差异化角色；
- [ ] 长中文文案不会溢出；
- [ ] 图片失败时布局仍成立；
- [ ] 动效不会造成页面跳动；
- [ ] 手机端主操作始终容易触达。

### 16.3 可访问性

- [ ] 所有交互可以使用键盘完成；
- [ ] 焦点样式清晰；
- [ ] 输入有可访问名称；
- [ ] 非装饰图片有 alt；
- [ ] 动态状态使用适当的 live region；
- [ ] 不只依赖颜色传达状态；
- [ ] 支持减少动态效果；
- [ ] 文本对比度基本达标。

### 16.4 工程

- [ ] TypeScript 无错误；
- [ ] 构建成功；
- [ ] 测试通过；
- [ ] lint 通过；
- [ ] 无 API Key 进入仓库；
- [ ] Demo 数据与 UI 分离；
- [ ] 核心状态转换可测试；
- [ ] 没有明显控制台报错。

---

## 17. README 必须包含

Code Agent 最终应创建项目 README，至少包含：

1. 产品截图或 GIF；
2. 一句话介绍；
3. 核心体验；
4. 为什么不是普通旅游攻略生成器；
5. 技术栈；
6. 本地启动命令；
7. Demo 模式说明；
8. 真实 AI / 工具接入方式；
9. Agent 工作流图；
10. 评价与安全设计；
11. 当前限制；
12. 后续路线图。

---

## 18. 面试呈现方式

### 18.1 90 秒 Demo 脚本

1. 说明问题：很多人想旅行，但不知道想去哪里，只知道想进入什么状态；
2. 输入案例 A；
3. 展示 AI 将模糊感受翻译成结构化氛围；
4. 展示 Agent 搜索与现实核验步骤；
5. 对比三个差异化目的地；
6. 点击“再安静一点”，展示二次探索；
7. 展开函馆三日提案；
8. 结束语：产品不替用户生成标准攻略，而是把感觉连接到真实世界。

### 18.2 项目讲解重点

- 为什么选择“感觉”作为输入，而不是目的地；
- 为什么先让用户确认 AI 的理解；
- 为什么结果必须展示取舍；
- Agent 如何拆分任务和使用工具；
- 如何评价“推荐得好不好”；
- 如何避免实时信息和旅行事实的幻觉；
- 哪些信息来自模型、哪些来自工具、哪些需要用户再次确认。

---

## 19. 最终 Definition of Done

只有同时满足以下条件，项目才算完成：

- [ ] 是一个可以真实运行的 Web 项目；
- [ ] 默认无需 API Key 即可完成完整 Demo；
- [ ] 用户可以从输入走到详情，不是静态展示；
- [ ] 至少有 3 组演示案例；
- [ ] 至少有一次可交互的氛围微调；
- [ ] 三个目的地结果有不同推荐角色；
- [ ] 每个结果同时说明匹配理由和现实取舍；
- [ ] 页面有明确而统一的视觉方向；
- [ ] 桌面端与移动端可用；
- [ ] 加载、空结果、错误状态齐全；
- [ ] 支持 reduced-motion；
- [ ] 核心流程有自动化测试；
- [ ] 构建、测试、lint 通过；
- [ ] README 足以让陌生评审理解产品和运行项目；
- [ ] 最终交付包含截图或短录屏；
- [ ] Code Agent 已对照本文件完成逐项自查。

---

## 20. 最后一条产品提醒

这个项目最容易失败的方式，是做成一个很漂亮的旅行 Landing Page，或者做成一个会生成诗意文案的聊天框。

真正需要被实现的产品瞬间是：

> 用户说出一种难以描述的感觉，AI 准确地理解它，在真实世界中找到几个意外但合理的地方，并诚实地说明每个选择的依据与代价。

所有功能、视觉和技术取舍都应服务于这个瞬间。
